// Site Configuration
// These values are available in all templates via {{key}} or {{ctx.key}}

pub const site_name = "My Awesome Site";
pub const base_url = "";
pub const author = "waffles";
pub const description = "A static site built with Zig";

// Directories (relative or absolute paths)
pub const content_dir = "../bookcontents";
